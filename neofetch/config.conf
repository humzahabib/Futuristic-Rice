# Neofetch config file
# https://github.com/dylanaraps/neofetch

# See this wiki page for more info:
# https://github.com/dylanaraps/neofetch/wiki/Customizing-Info

print_info() {
    
    info "${c3}OS" distro
    info "${c3}Uptime" uptime
    info "${c3}Packages" packages
    info "${c3}Shell" shell
    info "${c3}Resolution" resolution
    info "${c3}WM" wm
    info "${c3}Theme" theme
    info "${c3}Icons" icons
    info "${c3}Terminal" term
    info "${c3}Terminal Font" term_font

    # info "GPU Driver" gpu_driver  # Linux/macOS only
    # info "CPU Usage" cpu_usage
    # info "Disk" disk
    # info "Battery" battery
    # info "Font" font
    # info "Song" song
    # [[ "$player" ]] && prin "Music Player" "$player"
    # info "Local IP" local_ip
    # info "Public IP" public_ip
    # info "Users" users
    # info "Locale" locale  # This only works on glibc systems.

    info line_break
    info cols
    info line_break
}

# Title
title_fqdn="off"

# Kernel
kernel_shorthand="on"

# Distro
distro_shorthand="on"
os_arch="on"

# Uptime
uptime_shorthand="on"

# Memory
memory_percent="on"
memory_unit="gib"

# Packages
package_managers="on"

# Shell
shell_path="off"
shell_version="on"

# CPU
speed_type="bios_limit"
speed_shorthand="on"
cpu_brand="on"
cpu_speed="on"
cpu_cores="logical"
cpu_temp="off"

# GPU
gpu_brand="on"
gpu_type="all"

# Resolution
refresh_rate="off"

# Desktop Environment
de_version="on"

# Colors
colors=(5 1 5 1 5 7)
bold="on"
underline_enabled="on"
underline_char="-"

# Info Separator
separator="  "

# Color Blocks
block_range=(0 15)
color_blocks="on"
block_width=3
block_height=1
col_offset="auto"

# Progress Bars
bar_char_elapsed="-"
bar_char_total="="
bar_border="on"
bar_length=15
bar_color_elapsed="distro"
bar_color_total="distro"

# Info display
cpu_display="off"
memory_display="off"
battery_display="off"
disk_display="off"

# Backend Settings
image_backend="ascii"
image_source="$HOME/.config/neofetch/cyberpunk_ascii.txt"

# Ascii Options
# Default is to use distro ascii art, but we'll use our custom art
ascii_distro="auto"
ascii_colors=(5 6)
ascii_bold="on"

# Image Options
image_loop="off"

# Crop mode
crop_mode="normal"
crop_offset="center"

# Gap between image and text
gap=2

# Image size
image_size="auto"

# Misc Options
stdout="off"
